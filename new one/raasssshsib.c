#include<stdio.h>
#include<conio.h>

int main() {
    printf("Hello, World!\n");
    getch(); // Wait for a key press
    return 0;
}
// This is a simple C program that prints "Hello, World!" to the console.
// It uses the C17 standard and includes necessary headers for input/output operations.
// The program will wait for a key press before exiting, allowing the user to see the output.
#include <stdio.h>
#include <conio.h>                  
// This program prints "Hello, World!" to the console and waits for a key press before exiting.
